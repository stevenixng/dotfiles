- defaults:
    link:
      relink: true

- clean: ['~', '~/bin']

- link:
    ~/.vimrc: vimrc
    ~/.gitconfig: gitconfig
    ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    - ~/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
