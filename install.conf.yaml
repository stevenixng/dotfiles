- defaults:
    link:
      relink: true

- clean: ['~', '~/bin']

- link:
    ~/.vimrc: vimrc
    ~/.gitconfig: gitconfig

- create:
    - ~/downloads
    - ~/.vim/undo-history
    - ~/.vim: vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
